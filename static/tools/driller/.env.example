# AI Configuration
# Set to 'on' to use external AI providers, 'off' for local mock (default)
VITE_AI_REMOTE=off

# AI Provider: 'mock', 'openai', 'anthropic'
VITE_AI_PROVIDER=mock

# AI Model (when using external providers)
VITE_AI_MODEL=

# API Keys (when using external providers)
VITE_OPENAI_API_KEY=
VITE_ANTHROPIC_API_KEY=