apiVersion: chaos-mesh.org/v1alpha1
kind: PodChaos
metadata:
  name: kill-echo
  namespace: chaos-testing
spec:
  action: pod-kill
  mode: one
  duration: "1m"
  selector:
    namespaces: [ "echo" ]
    labelSelectors:
      app: echo
